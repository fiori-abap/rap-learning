projection;
strict;
use draft;

define behavior for ZC_RAP_T006_H
{
  use create;
  use update;
  use delete;

  use association _Items
  {
    create;
    with draft;
  }

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}

define behavior for ZC_RAP_T006_I
{
  use update;
  use delete;

  use association _Header
  {
    with draft;
  }
}